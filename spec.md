# データベース構成
## GoogleカレンダーキャッシュDB
- privarte/cache/month-yyyy-mm.txt（既存）
- GoogeleカレンダーAPIを通してイベントを取得した場合に月毎に更新する。
## 共通設定DB
- private/data/config.txt（既存）
- 現状のパラメータに加え、本アプリで有効な前後の月数を追加する。管理画面で設定。
  - 過去の有効月数（例:有効月数が2で、現在が8月の場合、6月まで表示できる）
  - 未来の有効月数（例:有効月数が2で、現在が8月の場合、10月まで表示できる）
- ファイルが存在しない場合は新規作成する。
## カレンダーDB
- private/data/calendars.txt（既存）
- ファイルが存在しない場合は新規作成する。
## 休日DB
- private/data/holidays.txt（既存）
- GoogeleカレンダーAPIを通してイベントを取得した場合に更新する。
- ファイルが存在しない場合は新規作成する。
## 画像DB
- private/data/images.txt（既存）
- ファイルが存在しない場合は新規作成する。
## イベント詳細DB
- private/data/events-yyyy-mm.txt（新規作成）
- イベント管理画面 manage_events.php で設定した内容を月単位で上書き保存する。
- 最低限必要な項目は以下。
  - イベントID（GoogleカレンダーのイベントID）
  - イベント種別（種別コードを設定）
  - 参加目的種別（種別コードを設定。カンマ区切りで複数指定可）
  - 簡易タイトル
  - 簡易説明
  - 日付
  - 開催時間
  - 開始時刻
  - 場所
  - 定員
  - 残り人数
  - 残り人数公開フラグ（デフォルトfalse）
  - 参加費
  - 申し込みURL
  - 申し込み締め切り日時
  - 対象者
  - 参加メリット
  - メイン画像ID（imagesのID）
  - 文頭画像ID（imagesのID）
  - 文末画像ID（imagesのID）
  - おすすめフラグ（デフォルトfalse）
## イベント一覧キャッシュDB
- private/data/event_cache.txt（新規作成）
- いずれかのイベント詳細DBを更新したタイミングで更新する。
- イベント詳細DBから当日以降のイベント情報を抜粋して保存する。
- 最低限必要な項目は以下。
  - イベントID（GoogleカレンダーのイベントID）
  - イベント種別
  - 参加目的種別
  - 簡易タイトル
  - 簡易説明
  - 日付
  - 開始時刻
  - 場所
  - 定員
  - 残り人数
  - 残り人数公開フラグ（デフォルトfalse）
  - 参加費
  - 申し込みURL
  - 申し込み締め切り日時
  - 対象者
  - 参加メリット
  - メイン画像ID（imagesのID）
  - おすすめフラグ（デフォルトfalse）
## カレンダーキャッシュDB
- private/data/calender_cache.txt（新規作成）
- いずれかのイベント詳細DBを更新したタイミングで更新する。
- イベント詳細DBから、共通設定DBで設定した過去/未来有効期間のイベント情報を抜粋して保存する。ただし前後6日分ずつを含める。例：過去2ヶ月、未来3ヶ月、現在8月の場合、5/26から11/6までを保存する。
- 最低限必要な項目は以下。
  - イベントID（GoogleカレンダーのイベントID）
  - イベント種別
  - 簡易タイトル
  - 日付
  - 開始時刻

# 削除するDB
以下のDBファイルを実装の最後に、処理側含め削除する
- private/data/event_details.txt
- private/data/event_template.txt

# 種別コード
DBに保存するコードと表示名を以下の通り定義する。独立したファイルで定義すること。
## イベント種別
- study:勉強会
- networking:交流会
- seminar:セミナー
- challenge:チャレンジ
- online:オンライン
## 参加目的種別
- networking:手軽に交流したい
- connections:人脈を増やしたい
- promotion:事業や自分をPRしたい
- startup:起業したい
- study:ビジネスを学びたい
- sports:体を動かしたい

# ページ構成と処理
## メインページ
- index.php（既存）
- 以下の構成と仕様に変更する。
### ヘッダーセクション
- title.pngを少し小さくしてそれに合わせて表示エリアの高さを狭くする
### おすすめイベントセクション
- ヘッダーセッションの直下に移動させる。
- イベント一覧キャッシュDBから、本日以降の、おすすめフラグがtrueのイベントを日時昇順にカード形式で表示。カードをタップするとイベント詳細画面に遷移。
- 表示項目は以下。
  - イベント種別アイコン（現状通り）
  - メイン画像（四角形で表示。画像ファイルが存在しない場合は非表示）
  - 日付（現状通り）
  - 開始時刻
  - 簡易タイトル
  - 簡易説明
  - 場所（未設定の場合は非表示）
  - 対象者（未設定の場合は非表示）
  - 参加メリット（未設定の場合は非表示）
  - 参加費（未設定の場合は非表示）
  - 定員と残り人数（未設定の場合は非表示、残り人数は残り人数公開フラグがtrueの場合のみ表示）
  - 申し込み締め切り日時（未設定の場合は非表示）
  - 「詳細を見る」ボタン（イベント詳細画面に遷移）
  - 「申し込む」ボタン（申し込みURLに遷移）
### イベント検索セクション
- 新規作成。
- 「イベント種別選択フォーム」「参加目的選択フォーム」「検索ボタン」を1行で配置。
- イベント種別選択フォームは、「種別選択」およびイベント種別からドロップダウンで選択できる。
- 参加目的選択フォームは、「参加目的選択」および参加目的種別からドロップダウンで選択できる。
- 検索ボタンを押下したら、URLにイベント種別コードと参加目的コードをパラメータとして追加して検索ページに遷移する。未選択の場合はパラメータを追加しない。
### カレンダーセクション
- 現状のカレンダーを表示。
- カレンダーキャッシュDBをもとにイベントを表示する。
- イベントは、簡易タイトルと開始時刻のみ表示する。
- イベントを押下したらイベント詳細画面に遷移。
- 既存処理通り、表示した月のGoogleカレンダーをAPIで取得しGoogleカレンダーキャッシュDBを上書き保存。その後、カレンダーキャッシュDBを最新化。
- カレンダーキャッシュDBは、全GoogleカレンダーキャッシュDBのうち共通設定DBで設定されている有効範囲（前後6日間ずつ含む）のイベントを反映。
### イベント一覧セクション
- イベント一覧キャッシュDBから、本日以降のイベントを日時昇順にカード形式で表示。カードをタップするとイベント詳細画面に遷移。
- スタイルはおすすめイベントと共通。
- 表示項目は以下。
  - イベント種別アイコン
  - メイン画像（四角形で表示。画像ファイルが存在しない場合は非表示）
  - 日付
  - 開始時刻
  - 簡易タイトル
  - 簡易説明

## イベント詳細ページ
- ヒーロー部の画像は、イベント詳細DBのメイン画像を表示。画像ファイルが存在しない場合は現状通り people.png を表示。
- ヒーロー部のタイトルは、GoogleカレンダーキャッシュDBのタイトルを表示。
- ヒーロー部の説明文は、イベント詳細DBの簡易説明を表示。
- イベント詳細DBの文頭画像IDが設定されている場合はイベント詳細分の冒頭に対象画像を表示。画像ファイルが存在しない場合は非表示。
- イベント詳細DBの文末画像IDが設定されている場合はイベント詳細分の末尾に対象画像を表示。画像ファイルが存在しない場合は非表示。

## 検索ページ
- 新規作成。
- ヘッダーは詳細ページと同じスタイルで、戻るボタンとロゴを表示。
- ヘッダーの下に検索フォーム。メインページのものと同じで、URLパラメータで指定されている種別を選択した状態。
- 検索フォームの下にイベント一覧。URLパラメータで指定されているイベント種別と参加目的種別が設定されているイベントのみを日時昇順で表示する。イベント種別と参加目的種別はアンド検索。
- イベントの表示スタイルは、メインページのイベント一覧セクションと共通。

## 管理ページ
- 一番上に「共通設定」ボタンを追加。

## 共通設定ページ
- 新規作成。
- 左上に戻るボタン（他ページと同様）
- private/data/config.txt の項目を表示、編集する。
- 保存ボタンあり。保存結果は保存ボタンの右側に表示。

## カレンダー設定ページ
- 変更なし。

## 画像管理ページ
- 変更なし。

## イベント管理ページ
- URLの末尾に年月をパラメータとして付加し、その月の設定を開く。デフォルトは当月。例：date=202608
- 年月選択フォームは、共通設定DBで指定した有効範囲プラス1月分の範囲で、年月をセットで選択できるように変更。例：当月2026年8月、過去範囲2ヶ月、未来範囲4ヶ月の場合、「2026年5月」から「2027年1月」まで選択できる。
- 画面描画した際、ローディングインジケータを表示し操作できないようにする。その間、GoogleカレンダーAPIで対象月のイベントを取得しGoogleカレンダーキャッシュDBを上書き保存。その後インジケータ非表示。
- 対象月のGoogleカレンダーキャッシュDBに保存されているイベントのフォームを表示。イベント詳細DBに保存されている値があれば不フォームに反映。
- フォーム項目は以下。
  - コピー元
  - イベント種別（ドロップダウンで選択）
  - 参加目的種別（チェックボックスで複数選択）
  - 簡易タイトル
  - 簡易説明
  - 開催時間
  - 開始時刻
  - 場所
  - 定員
  - 残り人数
  - 残り人数公開フラグ（デフォルトfalse）
  - 参加費
  - 申し込みURL
  - 申し込み締め切り日時
  - 対象者
  - 参加メリット
  - メイン画像（オリジナルファイル名をドロップダウンで選択）
  - 文頭画像（オリジナルファイル名をドロップダウンで選択）
  - 文末画像（オリジナルファイル名をドロップダウンで選択）
  - おすすめフラグ（デフォルトfalse）
- 保存ボタンの下の保存結果は保存ボタンの右側に表示するよう変更。
- 保存ボタン押下で、入力内容を対象月のイベント詳細DBに上書き保存。同時に、イベント一覧キャッシュDBとカレンダーキャッシュDBを上書き保存。
- イベント一覧キャッシュDBは、その時点で全イベント詳細DBのうち当日以降のイベント情報を抜粋して保存する。
- カレンダーキャッシュDBは、全GoogleカレンダーキャッシュDBのうち共通設定DBで設定されている有効範囲（前後6日間ずつ含む）のイベントを反映。保存されているイベント詳細DBの情報を反映。

filter: brightness(0) saturate(100%) invert(31%) sepia(94%) saturate(2464%) hue-rotate(201deg) brightness(95%) contrast(96%);